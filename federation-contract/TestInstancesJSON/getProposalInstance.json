{
    "fcn":"getProposal",
    "args":["proposal01"]
}
// it will returns a json of the proposal as it has been submitted, 
// but also with the "status" field. {["memberCloud", "proposal01", "In this test proposal, we want to propose...", "join", "unanimity", "3", "pending"]}
// to invoke it from terminal inside a peer in the docker cli bash:
// peer chaincode invoke -o orderer0:7050  --tls true --cafile $ORDERER_CA -C mychannel -n governance -c '{"Args":["getProposal","proposal01"]}' 
